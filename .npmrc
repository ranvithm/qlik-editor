# Registry configuration
registry=https://registry.npmjs.org/

# Always authenticate when publishing
always-auth=true

# Scope configuration for @ranjith packages
@ranjith:registry=https://registry.npmjs.org/

# Optional: Use npm audit to check for vulnerabilities
audit-level=moderate

# Optional: Save exact versions
save-exact=false